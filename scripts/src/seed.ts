/**
 * Development seed script — intentionally left empty.
 *
 * This file previously contained fake/mock referral data (Alice Johnson,
 * Bob Martinez, Carol Smith, etc.) that has been removed to prevent
 * accidental contamination of production data.
 *
 * To seed the offices table with real customer keys, use:
 *   pnpm --filter @workspace/scripts run seed-offices
 *
 * Do NOT add fake patient or event arrays back here.
 */

console.log("No seed data to apply. Use 'seed-offices' to configure office customer keys.");
process.exit(0);
